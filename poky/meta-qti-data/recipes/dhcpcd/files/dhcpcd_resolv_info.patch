Index: dhcpcd-5.2.10/dhcpcd-hooks/20-resolv.conf
===================================================================
--- dhcpcd-5.2.10.orig/dhcpcd-hooks/20-resolv.conf
+++ dhcpcd-5.2.10/dhcpcd-hooks/20-resolv.conf
@@ -61,7 +61,7 @@ build_resolv_conf()
 	else
 		echo "# /etc/resolv.conf.tail can replace this line" >> "$cf"
 	fi
-	cat "$cf" > /etc/resolv.conf
+	cat "$cf" >> /etc/resolv.conf
 	chmod 644 /etc/resolv.conf
 	rm -f "$cf"
 }
@@ -116,6 +116,9 @@ remove_resolv_conf()
 			rm -f "$resolv_conf_dir/$interface"
 		fi
 		build_resolv_conf
+		sed  '/Generated by dhcpcd/,/resolv.conf.tail/d' /etc/resolv.conf >/tmp/resolv.conf.tmp
+               cat /tmp/resolv.conf.tmp >/etc/resolv.conf
+
 	fi
 }
 
